---
description: Compatibility shim for older agents that still follow the security-audit workflow.
---

# Compatibility Workflow: Security Audit

This workflow name is retained for backward compatibility.

Use `.agents/skills/security-scanner/SKILL.md` as the canonical security audit guide.

If an agent was told to follow `security-audit.md`, load the scanner skill above and apply the referenced personas.
